<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="co.anode.anodium">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/anode"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/anode"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="q">
        <activity android:name="co.anode.anodium.DebugActivity"/>
        <activity android:name="co.anode.anodium.VerificationActivity" />
        <activity android:name="co.anode.anodium.SignInActivity" />
        <activity android:name="co.anode.anodium.AccountMainActivity" />
        <activity android:name="co.anode.anodium.AccountNicknameActivity" />
        <activity
            android:name="co.anode.anodium.VpnListActivity"
            android:label="@string/title_activity_vpn_servers_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="co.anode.anodium.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name="co.anode.anodium.AnodeVpnService"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>
    </application>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

</manifest>